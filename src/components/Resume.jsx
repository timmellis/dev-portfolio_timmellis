import React from 'react';

function Resume(props) {
  return (
    <div className='section-container resume-container' id={props.id}>
      <div className='block-center'>
        This is the resume section.
      </div>

    </div>
  );
}

export default Resume;